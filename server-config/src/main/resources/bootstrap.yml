server:
  port: 8888

eureka:
  client:
    serviceUrl:
      defaultZone: http://118.194.51.250:8761/eureka/

spring:
  application:
    name: server-config
  profiles:
    active: native
  cloud:
    config:
      server:
        native:
          search-locations: classpath:config/